<!DOCTYPE html>
<html>
  <head>
    <title>Controlfinal</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

   <body>

      <h1><b>Cafe critic<b></h1>
  <nav class="navbar navbar-default navbar-fixed-top>
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
        <a class="navbar-brand" href='http://localhost:3000'>All places</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
          <% if user_signed_in? %>
              <li class="active"><a href="">Hi, <%=current_user.name %>!<span class="sr-only">(current)</span></a></li>
              <li class="active"><%= link_to 'Sign out', destroy_user_session_path, method: :delete %></li>
              <li class="active"><%= link_to 'Add new place', new_place_path %></li>
          <% else %>
              <li class="active"><%= link_to 'Sign up', new_user_registration_path %></li> 
              <li class="active"><%= link_to 'Log in', new_user_session_path %></li>
          <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
   <%= yield %>
  </body>
</html>