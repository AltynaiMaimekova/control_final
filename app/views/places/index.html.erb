<p id="notice"><%= notice %></p>

<h1>All Places</h1>

<p id="notice"><%= notice %></p>
<div class="container-fluid">
 <span>Select category</span><br>
  <div class="row">
    <div class="col-sm-3 col-lg-3">
      <nav class="navbar navbar-default navbar-fixed-side">
         <ul class="w3-bar-item w3-button"><li><%= link_to 'All', root_path %></li></ul>
         <% @categories.each do |category| %>
           <ul class="w3-bar-item w3-button">
              <li><%= link_to category.name, category %></li>
           </ul>
         <% end %>
     </nav>
    </div>
    <div class="col-sm-9 col-lg-9">
      <% @places.each do |place| %>
        <div class = "notice">
          <%= link_to (image_tag place.image.url(:medium)), place_path(place) %><br>
<!--           <b><span style="font-size:15px;">Category: <%= link_to place.category.name, category_path(place.category_id) %></span></b><br> -->
          <span style="font-size:20px;"><%= link_to place.title, place %></span><br>
          <span>(<%= average_score(place)%>, <%= place.reviews.length %> reviews)</span><br>
          <span><%= place.pictures.length %> photos</span>
<!--           <p>by <%=place.user.name %></p> -->
        </div><br>
      <% end %>
    </div> 
  </div>
</div>





<!-- <table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th>Agreement</th>
      <th>Status</th>
      <th>Category</th>
      <th>User</th>
      <th>Image</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @places.each do |place| %>
      <tr>
        <td><%= place.title %></td>
        <td><%= place.description %></td>
        <td><%= place.agreement %></td>
        <td><%= place.status %></td>
        <td><%= place.category.name %></td>
        <td><%= place.user.name %></td>
        <td><%= image_tag place.image.url(:thumb) %></td>
        <td><%= link_to 'Show', place %></td>
        <td><%= link_to 'Edit', edit_place_path(place) %></td>
        <td><%= link_to 'Destroy', place, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table> -->

<br>

<%= link_to 'New Place', new_place_path %>
